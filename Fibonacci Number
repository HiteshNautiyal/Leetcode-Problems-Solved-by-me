class Solution {
public:
    int fib(int n) {
        
        //base case
        if(n==0 or n==1)
        return n;

        int ans = fib(n-1) + fib(n-2);//recursive tree
        return ans;
    }
};
